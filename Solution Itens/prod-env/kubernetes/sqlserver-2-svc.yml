apiVersion: v1
kind: Service
metadata:
  name: sqlserver-svc
spec:
  selector:
    app: sqlserver-pod
  ports:
  - protocol: TCP
    port: 1433
    targetPort: 1433
    nodePort: 31433
  type: NodePort
apiVersion: apps/v1
kind: Deployment
metadata:
  name: contatos-persistenciaservice-deployment
  labels:
    app: arquivo-persistenciaservice-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: contatos-app
  template:
    metadata:
      labels:
        app: contatos-app
    spec:
      containers:
    - name: contatos-persistenciaservice-container
      image: rodrigocezarleao/contatos.persistenciaservice:v2
        ports:
        - containerPort: 8080